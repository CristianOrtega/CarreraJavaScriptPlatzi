var nombre = 'Cristian', edad = 28;

function imprimirEdad(name, age) {
    console.log(`${name} tiene ${age} años`);
}

imprimirEdad(nombre, edad);
imprimirEdad('Vicky', 28);
imprimirEdad('Eric', 24);