var frutas = ['Manzana', 'Peras', 'Banana', 'Cereza'];

console.log(frutas.length); // Cantidad de elementos

console.log(frutas[1]); // Accedes a un elemento del array, segundo el valor que le ingreses. En este caso muestra el segundo elemento.

frutas.push('Limon'); // Agrega un nuevo elemento al final de array.

frutas.pop(); // Elimina el último elemento.

frutas.unshift('Tomate'); // Agrega un elemento al inicio del array

frutas.shift(); // Elimina el primer elemento del array.

frutas.indexOf(); // Busca un elemento